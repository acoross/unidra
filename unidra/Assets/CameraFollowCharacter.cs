using UnityEngine;
using System.Collections;

public class CameraFollowCharacter : MonoBehaviour {

    public GameObject myFollowingCharacter;

    // Use this for initialization
    void Start() {

    }

    // Update is called once per frame
    void Update() {
//        transform.position = myFollowingCharacter.transform.position;
//        transform.Translate(new Vector3(0f, 2f, -3f));
    }
}
